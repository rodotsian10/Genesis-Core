[package]
name = "genesis_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "genesis_core"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
